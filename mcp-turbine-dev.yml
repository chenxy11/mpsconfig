#指明监控的服务
turbine:
    appconfig: mcp-auth
    aggregator:
          clusterConfig: default
    clusterNameExpression: new String("default")
    instanceUrlSuffix: 6001/hystrix.stream
feign:
    hystrix:
        enabled: true